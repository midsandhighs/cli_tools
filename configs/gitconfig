[includeIf "gitdir:~/src/"]
  	path = .gitconfig-mids      
[includeIf "gitdir:~/src/thd/"]
  	path = .gitconfig-thd  
[filter "media"]
	required = true
	clean = git media clean %f
	smudge = git media smudge %f
[push]
	default = matching
[core]
	editor = vim